/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['assets-global.website-files.com', 'tierforge.s3.us-west-1.amazonaws.com']
    },
    reactStrictMode: false
};

export default nextConfig;